#pragma once
#include "SDL.h"
#include <vector>	

// struct DebugJob {
// 	std::vector<b2Vec2> polygon;
// 	Color color;
// };

class Debugger
{
private:
	// SDL_Renderer* renderer;
	// std::vector<DebugJob> debugJobs;
	// void drawPolygon(std::vector<b2Vec2>);
public:
	// Debugger(SDL_Renderer*);
	// ~Debugger();
	// void drawAll();
	// void submitJob(DebugJob);
};