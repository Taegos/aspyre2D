#include "physics_system.h"

// PhysicsSystem::PhysicsSystem(TransformSystem& _transformSystem) : transformSystem{ _transformSystem } 
// {
// }

void PhysicsSystem::update(float dt)
{
	//world.Step(dt, 6, 2);
}